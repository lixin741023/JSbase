// ECMAScript 通过 RegExp类型 来表示正则表达式。
// 结构：RegExpExpression = / pattern / flags ;

// 其中 pattern 部分是：简单或复杂的正则表达式；
// flags 是标志，1个正则表达式可以带一个或多个标志。

// flags 有：
// g(global) i(case-insensitive) m(multiline)

